============
Installation
============

At the command line::

    $ easy_install django-research-projects

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-research-projects
    $ pip install django-research-projects
